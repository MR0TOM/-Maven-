server:
  port: 8080

spring:
  # 开发环境数据源
  datasource:
    url: jdbc:mysql://localhost:3306/smart_park_energy_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

  # 开发环境 Redis
  data:
    redis:
      host: localhost
      port: 6379

# 日志级别
logging:
  level:
    com.smartpark: debug
    org.springframework.web: debug
