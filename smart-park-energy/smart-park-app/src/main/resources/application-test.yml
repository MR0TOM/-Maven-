server:
  port: 8080

spring:
  # 测试环境数据源
  datasource:
    url: jdbc:mysql://test-mysql:3306/smart_park_energy_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: test
    password: test

  # 测试环境 Redis
  data:
    redis:
      host: test-redis
      port: 6379

# 日志级别
logging:
  level:
    com.smartpark: info
    org.springframework.web: warn
